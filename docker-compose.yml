version: '3.8'

services:
  telegram-forwarder:
    container_name: telegram-forwarder
    image: ghcr.io/journey-ad/telegram-forwarder:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # Environment variables file
      - ./.env:/app/.env
      # Configuration directory
      - ./config:/app/config
      # Session file persistence
      - ./sessions:/app/sessions
      # Log persistence
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
